// Direct fix for the SoftwareComponent issue in expo-modules-core

// This function should be used in place of the problematic components.release
def getSoftwareComponent(components) {
    def releaseComponent = components.findByName("release")
    if (releaseComponent != null) {
        return releaseComponent
    }
    return components.getByName("default")
} 